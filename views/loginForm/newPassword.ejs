<!DOCTYPE html>
<html lang="en">

    <head>

        <title><%= config.siteNavbar %> | New Password</title>

        <%- include('./partials/header') %>

    </head>

    <body>

        <!-- Top content -->
        <div class="top-content">
          
            <div class="inner-bg">
                <div class="container">
                    <div class="row">
                        <div class="col-sm-8 col-sm-offset-2 text">
                            <h1><strong><%= config.siteName %> </strong> Login Form</h1>
                            <div class="description">
                              <p>
                                    <!-- error message -->
                                    <% if (auth.locals.status == -1) { %>
                                         <div class="alert alert-danger">
                                            <strong>Error!</strong> <%= auth.locals.msg %>
                                        </div>                                   
                                    <% } %>
                                    <!-- end error message -->
                              </p>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-6 col-sm-offset-3 form-box">
                          <div class="form-top">
                            <div class="form-top-left">
                              <h3>New Password</h3>
                                <p>Set your new password here</p>
                            </div>
                            <div class="form-top-right">
                              <i class="fa fa-lock"></i>
                            </div>
                            </div>
                            <div class="form-bottom">
                          <form role="form" action="/auth/newPassword" method="post" class="login-form">                             
                            <div class="form-group">
                              <label class="sr-only" for="form-username">Username</label>
                                <input type="text" name="email" placeholder="Email..." value="<%= email %>" class="form-username form-control" readonly id="form-username">
                              </div>
                              <div class="form-group">
                                <label class="sr-only" for="form-password">Password</label>
                                <input type="password" name="password" placeholder="Password..." class="form-password form-control" id="form-password">
                              </div>                                  
                              <button type="submit" class="btn">Update!</button>
                          </form>
                        </div>
                        </div>
                    </div>
                </div>
            </div>
            
        </div>


        <%- include('./partials/javascript') %>

    </body>

</html>